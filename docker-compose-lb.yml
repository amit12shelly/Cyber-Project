version: '3.8'

services:
  load-balancer-server:
    build: ./loadBalancer
    restart: always
    ports:
      - "8080:8080"
    environment:
      - LOGIN_IP=192.168.1.156
    extra_hosts:
      - "game-server:192.168.1.156"
      - "login-server:192.168.1.156"